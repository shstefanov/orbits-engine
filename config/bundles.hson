{

  "DEFAULT_ROUTES": {
    "":                                 "setContext",
    "/":                                "setContext",
    "/:screen" :                        "setContext",
    "/:screen/:tab" :                   "setContext",
    "/:screen/:tab/:context" :          "setContext",
    "/:screen/:tab/:context/:action" :  "setContext"
  },

  "ROUTE_STATE_PARAMS": [
    "screen", "tab", "context", "action", "param_1", "param_2", "param_3", "param_4"
  ],

  "USER_ROLES": ["admin", "player", "operator", "editor", "developer"],

  "STATE_VAR": "state",

  "ADMIN_CONFIG": {
    "debug":         false
  },
  
  "GAME_CONFIG": {
    "debug":         false
  },

  "EDITOR_CONFIG": {
    "debug": false
  },

  "PANEL_CONFIG": {
    "debug": false
  },

  "DEV_CONFIG": {
    "debug": false
  }
}